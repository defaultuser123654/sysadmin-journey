# sysadmin-journey
# Мой путь в sysadmin/DevOps

## День 1
- Установил Ubuntu Server 26.04 в UTM на Mac (ARM64)
- Освоил базовые команды: pwd, ls, cd, mkdir, touch
- Разобрался с правами доступа (drwxr-x---)
- Проверил systemctl status, нашёл ssh.service — работает
## День 1 (продолжение)
- Настроил SSH-доступ к серверу
- Успешно подключился с Mac через терминал: ssh user1@192.168.64.2
- Понял разницу между работой через консоль UTM и удалённым SSH-подключением
- дальше знаю что такое uptime - показывает общую информацию - сколько работает (1 час 34 минуты load leverage - показывает нагрузку 0 1 значит нулевая нагрузку  если показывает 1 значит на 100% загружен " дальше показывает сколько юзеров у меня 2 юзера потому что я подключился удаленно
- df -h - показывает диски
- uname - показывает информацию о системе
- узнал про sudo adducer ###
- sudo whoami - показывает root
- cat /etc/passwd | tail -5 - показывает последние 5 строк  файла со списком всех пользователей системы
- дополнительная информация  id testuser
- ls -la /home
- 
- touch → -rw-rw-r--
chmod 755 → -rwxr-xr-x
chmod 644 → -rw-r--r--
chmod 700 → -rwx------
chown testuser → владелец сменился на testuser
chown user1 → вернул обратно
## День 2
- start & enable : enable стоп+перезапуск одной командой используется для смены конфигурации 
